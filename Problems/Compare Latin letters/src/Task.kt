import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    // write your code here
    val x = scanner.next().first()
    val y = scanner.next().first()
    println(x.toUpperCase() == y.toUpperCase())
}